
	<h5>{{this.id}}. {{this.burger_name}}</h5>

	<button class="change-devoured" data-id="{{id}}" data-newDevoured="{{devoured}}">
		{{#if devoured}}Eaten Burgers!{{else}}Eat the burger{{/if}}
	</button>

	